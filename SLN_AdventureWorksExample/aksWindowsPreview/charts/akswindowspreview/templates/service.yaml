apiVersion: v1
kind: Service
metadata:
  name: {{ template "akswindowspreview.fullname" . }}
  labels:
    app: {{ template "akswindowspreview.name" . }}
    chart: {{ template "akswindowspreview.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "akswindowspreview.name" . }}
    release: {{ .Release.Name }}
